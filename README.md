# SudentManager

一个简陋的的基于链表的学生管理系统，目前还存在一些（很多）漏洞和没有捕获的异常。

运行后需要输入密码。

密码是：`lhm`

编译出的`StudentManager.exe`使用的参数是`-static -static-libgcc -static-libstdc++ -DNDEBUG -s -O3`，所以按理来说基本不需要安装其他运行库。
